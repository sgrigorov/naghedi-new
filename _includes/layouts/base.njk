{# Shared site chrome: header + footer come from partials, every page extends this. #}
<!doctype html>
<html lang="{{ site.lang }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
    <meta name="description" content="{{ description or site.description }}">
    <link rel="icon" type="image/x-icon" href="/assets/images/favicon.ico">
    <link rel="stylesheet" href="/assets/css/site.css">
    <link rel="stylesheet" href="/assets/css/static.css">
    <style>
      /* Squarespace hides these classes until its runtime JS renders them.
         We don't ship that JS, so reveal them unconditionally. */
      .background-image-fx,
      img[data-src]:not([src]):not([srcset]) { visibility: visible !important; }
    </style>
    {% if page.url != "/" %}
    <style>
      /* Footer is themed bright-inverse (white text) which works only over the
         home page's dark hero. On every other page the surrounding background
         is light, so force the footer text dark for legibility. */
      #footer-sections,
      #footer-sections p,
      #footer-sections h1, #footer-sections h2, #footer-sections h3,
      #footer-sections h4, #footer-sections h5, #footer-sections h6,
      #footer-sections li,
      #footer-sections span { color: #1a1a1a !important; }
      #footer-sections a { color: #1a3d8f !important; }
    </style>
    {% endif %}
    {% block head %}{% endblock %}
  </head>
  <body class="{{ site.bodyClass }}" tabindex="-1">
    <div id="siteWrapper" class="clearfix site-wrapper">
      {% include "partials/header.njk" %}
      {{ content | safe }}
      {% include "partials/footer.njk" %}
    </div>
    <script>
      // Squarespace CSS hides images until parent gets data-loaded.
      // Their loader JS used to set it; we don't ship the loader, so do it ourselves.
      (function () {
        function reveal() {
          document.querySelectorAll('img').forEach(function (img) {
            img.setAttribute('data-loaded', '');
            var p = img.parentElement;
            while (p && p !== document.body) {
              p.setAttribute('data-loaded', '');
              p = p.parentElement;
            }
          });
        }
        if (document.readyState === 'loading') {
          document.addEventListener('DOMContentLoaded', reveal);
        } else {
          reveal();
        }
      })();
    </script>
  </body>
</html>
